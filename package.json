{
  "name": "action-item-triage",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "bun run --cwd packages/server dev",
    "build": "bun run --cwd packages/web build",
    "test": "bun test"
  }
}
